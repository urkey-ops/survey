
{
  "name": "survey-app-server",
  "version": "1.0.0",
  "description": "A Vercel Function for the survey app.",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "dependencies": {
    "googleapis": "^142.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.18"
  },
  "scripts": {
    "tw:init": "npx tailwindcss init -p",
    "tw:build": "npx tailwindcss -i ./input.css -o ./dist/output.css --content './**/*.{html,js}' --minify",
    "tw:watch": "npx tailwindcss -i ./input.css -o ./dist/output.css --content './**/*.{html,js}' --watch"
  },
  "engines": {
    "node": "22.x"
  }
}

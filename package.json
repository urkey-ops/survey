
{
  "name": "survey-app-server",
  "version": "1.0.0",
  "description": "A Vercel Function for the survey app.",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "dependencies": {
    "googleapis": "^142.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3"
  },
  "scripts": {
    "tw:init": "./node_modules/.bin/tailwindcss init -p",
    "tw:build": "./node_modules/.bin/tailwindcss -i ./input.css -o ./style.css --content './**/*.{html,js}' --minify"
  },
  "engines": {
    "node": "22.x"
  }
}
